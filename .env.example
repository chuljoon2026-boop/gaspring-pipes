# Default root hosting (Vercel, localhost, Capacitor)
BASE_PATH=/

# GitHub project Pages example: BASE_PATH=/your-repository/
# This value is applied at build time; do not put secrets in VITE_* variables.
