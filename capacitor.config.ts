import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.gason.safetydemo',
  appName: '현장 조회',
  webDir: 'dist',
  backgroundColor: '#f5f7fa',
}

export default config
